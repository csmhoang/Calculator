<?xml version="1.0" encoding="UTF-8"?>

<Shell
    x:Class="Calculator.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:app="clr-namespace:Calculator"
    Shell.FlyoutBehavior="Disabled">
    <TabBar
        x:Name="Main"
        Route="main">
        <Tab
            Title="Admin"
            Route="admin">
            <ShellContent ContentTemplate="{DataTemplate app:AdminPage}" />
        </Tab>
        <!-- <Tab -->
        <!--     Title="Máy tính" -->
        <!--     Route="computor"> -->
        <!--     <ShellContent ContentTemplate="{DataTemplate app:ComputorPage}" /> -->
        <!-- </Tab> -->
        <!-- <Tab -->
        <!--     Title="Lịch sử" -->
        <!--     Route="history"> -->
        <!--     <ShellContent ContentTemplate="{DataTemplate app:HistoryCalculatorPage}" /> -->
        <!-- </Tab> -->
    </TabBar>
</Shell>